// D12 settings and integration presentation for the genuine Code OSS workbench.
// Rendering receives metadata-only fixture state; no secret value is ever rendered.

"use strict";

function renderSettings(state, currentNavigation, escapeHtml, escapeAttribute, humanize) {
  const section = currentNavigation === "database" ? "database" : currentNavigation === "integrations" ? "integrations" : state.section;
  return `<section class="settings-page" aria-labelledby="settings-title">
    <div class="page-heading"><div><p class="eyebrow">Desktop foundation</p><h1 id="settings-title">Settings and integrations</h1><p>Safe metadata and deterministic fixture actions. Account and billing authority remains in the Thrallo portal.</p></div><span class="state-label">Fixture only</span></div>
    <div class="settings-boundary" role="status" aria-live="polite"><strong>No live mutation path</strong><span>Secrets become metadata-only after submission. Database, provider, OAuth, and production environment changes remain unavailable.</span></div>
    <div class="settings-layout">
      <nav class="settings-sections" aria-label="Settings sections">${state.sections.map((id) => `<button type="button" data-settings-section="${escapeAttribute(id)}" aria-current="${id === section ? "page" : "false"}">${escapeHtml(sectionLabel(id))}</button>`).join("")}</nav>
      <div class="settings-content">${renderSection(section, state, escapeHtml, escapeAttribute, humanize)}</div>
    </div>
    ${renderSecretDeleteDialog(state, escapeHtml, escapeAttribute)}
  </section>`;
}

function renderSection(section, state, escapeHtml, escapeAttribute, humanize) {
  if (section === "overview") return renderOverview(state, escapeHtml, humanize);
  if (section === "account" || section === "usage") return renderPortalManaged(section, state, escapeHtml, escapeAttribute);
  if (section === "workspace") return renderWorkspace(state, escapeHtml, humanize);
  if (section === "desktop_local") return renderPreferences(state, escapeHtml, escapeAttribute, humanize);
  if (section === "environment") return renderEnvironment(state, escapeHtml, escapeAttribute, humanize);
  if (section === "secrets") return renderSecrets(state, escapeHtml, escapeAttribute, humanize);
  if (section === "database") return renderDatabase(state, escapeHtml, escapeAttribute, humanize);
  if (section === "stripe") return renderStripe(state, escapeHtml, escapeAttribute, humanize);
  if (section === "github") return renderGithub(state, escapeHtml, escapeAttribute, humanize);
  if (section === "ai_models") return renderModels(state, escapeHtml, escapeAttribute, humanize);
  if (section === "integrations") return renderIntegrations(state, escapeHtml, escapeAttribute, humanize);
  if (section === "storage") return renderStorage(state, escapeHtml, humanize);
  if (section === "security") return renderSecurity(state, escapeHtml, humanize);
  return `<div class="settings-card"><h2>${escapeHtml(sectionLabel(section))}</h2><p>This fixture section is unavailable.</p></div>`;
}

function renderOverview(state, escapeHtml, humanize) {
  const warnings = state.integrations.items.filter((item) => !["connected", "disconnected"].includes(item.status)).length;
  return `<div class="settings-stack"><section class="settings-card"><p class="eyebrow">Current context</p><h2>${escapeHtml(state.projectContext.name)}</h2><dl class="settings-meta"><div><dt>Workspace</dt><dd>${escapeHtml(humanize(state.projectContext.workspaceType))}</dd></div><div><dt>Git</dt><dd>${escapeHtml(humanize(state.projectContext.gitState))}</dd></div><div><dt>Network</dt><dd>${state.offline ? "Offline local mode" : "Fixture services available"}</dd></div><div><dt>Upload</dt><dd>Not uploaded</dd></div></dl></section>
    <section class="settings-summary" aria-label="Integration summary">${summary("Supabase", state.database.state, state.database.health, escapeHtml, humanize)}${summary("Stripe", state.stripe.state, state.stripe.webhookHealth, escapeHtml, humanize)}${summary("GitHub", state.github.state, state.github.remoteStatus, escapeHtml, humanize)}${summary("Warnings", String(warnings), state.integrations.stale ? "Health data is stale" : "Deterministic fixture status", escapeHtml, humanize)}</section>
    <section class="settings-card"><h2>Authority and scope</h2><p>Desktop-local preferences may be simulated here. Account, billing, subscription, usage authority, and API-token management remain portal-managed.</p><div class="button-row"><button type="button" data-settings-handoff="thrallo_account">Account portal</button><button type="button" data-settings-handoff="thrallo_billing">Billing portal</button><button type="button" data-settings-handoff="thrallo_integrations">Integration settings</button><button type="button" data-settings-handoff="thrallo_usage">Usage portal</button><button type="button" data-settings-handoff="thrallo_api_keys">API keys</button></div></section>
    ${renderAudit(state, escapeHtml, humanize)}</div>`;
}

function renderPortalManaged(section, state, escapeHtml, escapeAttribute) {
  const destination = section === "usage" ? "thrallo_usage" : "thrallo_account";
  return `<section class="settings-card"><p class="eyebrow">Portal managed</p><h2>${escapeHtml(sectionLabel(section))}</h2><p>Thrallo account ownership, subscription changes, billing recovery, usage authority, and API-token management remain on app.thrallo.com.</p><div class="button-row"><button type="button" data-settings-handoff="${escapeAttribute(destination)}">Open approved portal destination</button>${section === "account" ? `<button type="button" data-settings-handoff="thrallo_billing">Billing</button><button type="button" data-settings-handoff="thrallo_api_keys">API keys</button>` : ""}</div></section>`;
}

function renderWorkspace(state, escapeHtml, humanize) {
  const project = state.projectContext;
  return `<section class="settings-card"><p class="eyebrow">Safe local metadata</p><h2>Workspace</h2><dl class="settings-meta"><div><dt>Name</dt><dd>${escapeHtml(project.name)}</dd></div><div><dt>Type</dt><dd>${escapeHtml(humanize(project.workspaceType))}</dd></div><div><dt>Framework</dt><dd>${escapeHtml(project.framework)}</dd></div><div><dt>Git state</dt><dd>${escapeHtml(humanize(project.gitState))}</dd></div><div><dt>Environment files read</dt><dd>No</dd></div><div><dt>Uploaded</dt><dd>No</dd></div></dl><p class="settings-note">D8 exclusions remain authoritative: .env contents, credentials, Git internals, and private keys are not harvested.</p></section>`;
}

function renderPreferences(state, escapeHtml, escapeAttribute, humanize) {
  const items = state.preferences.filter((item) => ["desktop_local", "workspace_local"].includes(item.scope));
  return `<section class="settings-card"><p class="eyebrow">Local preferences</p><h2>Desktop and workspace</h2><form id="settings-preferences-form" class="preference-list">${items.map((item) => `<label><span><strong>${escapeHtml(item.id)}</strong><small>${escapeHtml(humanize(item.scope))} · ${escapeHtml(humanize(item.currentState))}</small></span><input type="checkbox" name="${escapeAttribute(item.id)}"${item.value === true ? " checked" : ""} data-preference-id="${escapeAttribute(item.id)}"></label>`).join("")}<button type="button" data-settings-action="save_preferences"${items.some((item) => item.dirty) ? "" : " disabled"}>Save fixture preferences</button></form></section>`;
}

function renderEnvironment(state, escapeHtml, escapeAttribute, humanize) {
  return `<div class="settings-stack"><section class="settings-card"><p class="eyebrow">Metadata only</p><h2>Project environments</h2><div class="environment-tabs">${state.environment.environments.map((item) => `<span>${escapeHtml(humanize(item))}</span>`).join("")}<span>Custom · integration pending</span></div><div class="metadata-table" role="table" aria-label="Environment variable metadata"><div role="row" class="table-head"><span>Key</span><span>Environment</span><span>Source</span><span>Status</span></div>${state.environment.variables.map((item) => `<div role="row"><code>${escapeHtml(item.key)}</code><span>${escapeHtml(humanize(item.environment))}</span><span>${escapeHtml(humanize(item.source))}</span><span>${item.secret ? "Secret · " : ""}${escapeHtml(humanize(item.validation))}</span></div>`).join("")}</div><p class="settings-note">Values are never returned. Inherited and overridden state is presentation metadata only.</p></section>
    <section class="settings-card"><h2>Preview variable metadata</h2><form id="environment-preview-form" class="compact-form"><label>Variable name<input name="key" required maxlength="80" pattern="[A-Z_][A-Z0-9_]+" autocomplete="off"></label><label>Environment<select name="environment">${state.environment.environments.map((item) => `<option value="${escapeAttribute(item)}">${escapeHtml(humanize(item))}</option>`).join("")}</select></label><label class="checkbox-label"><input type="checkbox" name="secret"> Secret metadata</label><button type="submit">Preview only</button></form>${state.environment.pendingPreview ? `<div class="settings-review"><strong>${escapeHtml(state.environment.pendingPreview.key)}</strong><span>Metadata preview · application unavailable</span></div>` : ""}</section></div>`;
}

function renderSecrets(state, escapeHtml, escapeAttribute, humanize) {
  return `<div class="settings-stack"><section class="settings-card"><p class="eyebrow">One-way input</p><h2>Secrets</h2><p>Values are accepted only for an explicit fixture create or replacement. Afterwards the controller returns metadata only; reveal and copy are unavailable.</p><div class="secret-list">${state.secrets.items.length ? state.secrets.items.map((item) => `<article><span class="status-glyph" aria-hidden="true">${item.state === "configured" ? "✓" : "!"}</span><div><strong>${escapeHtml(item.name)}</strong><small>${escapeHtml(humanize(item.class))} · revision ${item.revision}</small><small>${escapeHtml(humanize(item.state))} · value unavailable</small></div>${item.state !== "deleted" ? `<button type="button" class="danger" data-secret-delete="${escapeAttribute(item.name)}">Delete fixture metadata</button>` : ""}</article>`).join("") : `<p>No secret metadata is configured.</p>`}</div></section>
    <section class="settings-card"><h2>Create or replace synthetic secret</h2><form id="secret-form" class="compact-form"><label>Secret name<input name="name" required maxlength="80" pattern="[A-Z][A-Z0-9_]+" autocomplete="off" aria-describedby="secret-help"></label><label>Class<select name="secretClass">${state.secrets.classes.map((item) => `<option value="${escapeAttribute(item)}">${escapeHtml(humanize(item))}</option>`).join("")}</select></label><label>Secret value<input type="password" name="value" required minlength="8" maxlength="8192" autocomplete="new-password"></label><small id="secret-help">Use synthetic test data only. The value disappears from desktop state after submission.</small><div class="button-row"><button type="submit" name="operation" value="create">Create fixture metadata</button><button type="submit" name="operation" value="replace">Replace fixture metadata</button></div></form></section></div>`;
}

function renderDatabase(state, escapeHtml, escapeAttribute, humanize) {
  const db = state.database;
  return `<div class="settings-stack"><section class="settings-card"><div class="section-heading"><div><p class="eyebrow">Supabase fixture</p><h2>Database and Supabase</h2></div><span class="state-label">${escapeHtml(humanize(db.state))}</span></div><dl class="settings-meta"><div><dt>Project</dt><dd>${escapeHtml(db.projectLabel || "Not connected")}</dd></div><div><dt>Health</dt><dd>${escapeHtml(humanize(db.health))}</dd></div><div><dt>Migrations</dt><dd>${escapeHtml(humanize(db.migrationStatus))}</dd></div><div><dt>Storage</dt><dd>${escapeHtml(humanize(db.storageStatus))}</dd></div><div><dt>Auth</dt><dd>${escapeHtml(humanize(db.authCapability))}</dd></div><div><dt>RLS</dt><dd>${escapeHtml(humanize(db.rlsStatus))}</dd></div></dl><p class="settings-note">No service-role key, database password, privileged connection string, Studio embed, or SQL console is available.</p></section>
    <section class="settings-card"><h2>Schema summary</h2><div class="metadata-table" role="table" aria-label="Fixture database schema summary"><div role="row" class="table-head"><span>Table</span><span>Columns</span><span>Rows</span><span>RLS</span></div>${db.tables.map((item) => `<div role="row"><code>${escapeHtml(`${item.schema}.${item.name}`)}</code><span>${item.columns}</span><span>~${item.rowsApproximate}</span><span>${escapeHtml(humanize(item.rls))}</span></div>`).join("")}</div></section>
    <section class="settings-card"><h2>Structured change preview</h2><div class="change-list">${db.changes.map((item) => `<article><div><strong>${escapeHtml(humanize(item.operation))}</strong><small>${item.affectedObjects.map(escapeHtml).join(", ")} · ${escapeHtml(humanize(item.risk))}</small></div><button type="button" data-database-preview="${escapeAttribute(item.id)}">Review fixture change</button></article>`).join("")}</div>${db.pendingReview ? renderDatabaseReview(db.pendingReview, escapeHtml, humanize) : ""}</section></div>`;
}

function renderDatabaseReview(review, escapeHtml, humanize) { return `<div class="database-review" role="dialog" aria-labelledby="database-review-title" aria-describedby="database-review-detail"><p class="eyebrow">Explicit typed review</p><h3 id="database-review-title">${escapeHtml(humanize(review.operation))}</h3><p id="database-review-detail">This is a deterministic preview. No SQL will execute.</p><dl class="settings-meta"><div><dt>Affected objects</dt><dd>${review.affectedObjects.map(escapeHtml).join(", ")}</dd></div><div><dt>Risk</dt><dd>${escapeHtml(humanize(review.risk))}</dd></div><div><dt>Data loss warning</dt><dd>${review.dataLossWarning ? "Yes — destructive" : "No known destructive effect"}</dd></div><div><dt>Approval</dt><dd>Explicit typed approval required later</dd></div><div><dt>Validation</dt><dd>${escapeHtml(humanize(review.validation))}</dd></div><div><dt>Rollback</dt><dd>${escapeHtml(humanize(review.rollbackAvailability))}</dd></div></dl><button type="button" disabled>Apply unavailable until Track B</button></div>`; }

function renderStripe(state, escapeHtml, escapeAttribute, humanize) { const item = state.stripe; return `<section class="settings-card"><div class="section-heading"><div><p class="eyebrow">Synthetic metadata</p><h2>Stripe</h2></div><span class="state-label">${escapeHtml(humanize(item.state))}</span></div><dl class="settings-meta"><div><dt>Connection</dt><dd>${escapeHtml(item.connectionLabel || "Not connected")}</dd></div><div><dt>Mode</dt><dd>${escapeHtml(humanize(item.mode))}</dd></div><div><dt>Webhook health</dt><dd>${escapeHtml(humanize(item.webhookHealth))}</dd></div><div><dt>Project state</dt><dd>${escapeHtml(humanize(item.projectState))}</dd></div></dl><p>No secret key, product, price, customer, subscription, checkout session, or webhook endpoint is created.</p><div class="button-row"><button type="button" disabled>Connect unavailable</button><button type="button" data-settings-handoff="${escapeAttribute("stripe_manage")}">Open Stripe dashboard</button></div></section>`; }

function renderGithub(state, escapeHtml, escapeAttribute, humanize) { const item = state.github; return `<section class="settings-card"><div class="section-heading"><div><p class="eyebrow">Read-only fixture</p><h2>GitHub</h2></div><span class="state-label">${escapeHtml(humanize(item.state))}</span></div><dl class="settings-meta"><div><dt>Connection</dt><dd>${escapeHtml(item.accountLabel || "Not connected")}</dd></div><div><dt>Repository</dt><dd>${escapeHtml(item.repository || "Unavailable")}</dd></div><div><dt>Branch</dt><dd>${escapeHtml(item.branch || "Unavailable")}</dd></div><div><dt>Default branch</dt><dd>${escapeHtml(item.defaultBranch || "Unavailable")}</dd></div><div><dt>Local state</dt><dd>${item.dirty ? "Dirty" : "Clean fixture metadata"}</dd></div><div><dt>Permissions</dt><dd>${escapeHtml(humanize(item.permissions))}</dd></div></dl><p>Code OSS SCM remains the local Git authority. No commit, fetch, push, branch, PR, merge, or reset is automatic.</p><div class="button-row"><button type="button" disabled>Reconnect unavailable</button><button type="button" data-settings-handoff="${escapeAttribute("github_manage")}">GitHub installations</button></div></section>`; }

function renderModels(state, escapeHtml, escapeAttribute, humanize) { return `<section class="settings-card"><p class="eyebrow">D9 model configuration</p><h2>AI and model providers</h2><div class="provider-list">${state.modelProviders.map((item) => `<article><span class="status-glyph" aria-hidden="true">${item.state === "managed" || item.state === "byok_configured" ? "✓" : "!"}</span><div><strong>${escapeHtml(item.name)}</strong><small>${escapeHtml(humanize(item.classification))} · ${escapeHtml(humanize(item.state))}</small></div><button type="button" data-settings-section="secrets">${item.classification === "byok" ? "Manage synthetic key metadata" : "Managed by Thrallo"}</button></article>`).join("")}</div><p>No model is called. BYOK creation/replacement uses the one-way D12 secret contract.</p></section>`; }

function renderIntegrations(state, escapeHtml, escapeAttribute, humanize) { return `<div class="settings-stack"><section class="settings-card"><div class="section-heading"><div><p class="eyebrow">Host-neutral registry</p><h2>External integrations</h2></div><span>${state.integrations.stale ? "Stale read-only health" : "Fixture health"}</span></div><div class="integration-list">${state.integrations.items.map((item) => `<article><span class="status-glyph" aria-hidden="true">${item.status === "connected" ? "✓" : "!"}</span><div><strong>${escapeHtml(item.name)}</strong><small>${escapeHtml(humanize(item.status))} · ${escapeHtml(humanize(item.scope))} · checked ${escapeHtml(formatDateTime(item.lastChecked))}</small><small>${escapeHtml(humanize(item.authType))} · mutation ${escapeHtml(humanize(item.mutationAvailability))}</small></div><button type="button" data-integration-inspect="${escapeAttribute(item.id)}">Inspect</button></article>`).join("")}</div><div class="button-row"><button type="button" data-settings-handoff="thrallo_integrations">Thrallo integration settings</button></div></section>${renderAudit(state, escapeHtml, humanize)}</div>`; }

function renderStorage(state, escapeHtml, humanize) { const item = state.storage; return `<section class="settings-card"><p class="eyebrow">Presentation only</p><h2>Storage</h2><dl class="settings-meta"><div><dt>Local project</dt><dd>${item.local.available ? `${item.local.approximateBytes ?? "Unknown"} bytes` : "No local project selected"}</dd></div><div><dt>Import manifest</dt><dd>${item.local.importManifestBytes ?? "Unknown"}</dd></div><div><dt>Upload state</dt><dd>Not uploaded</dd></div><div><dt>Fixture cloud storage</dt><dd>${escapeHtml(humanize(item.fixtureCloud.state))}</dd></div><div><dt>Artifact usage</dt><dd>${item.fixtureCloud.artifactUsage?.bytes ?? "Unavailable"}</dd></div><div><dt>Cloud creation</dt><dd>Unavailable</dd></div></dl></section>`; }
function renderSecurity(state, escapeHtml, humanize) { return `<section class="settings-card"><p class="eyebrow">Fail closed</p><h2>Security boundaries</h2><dl class="settings-meta">${Object.entries(state.boundaries).filter(([,value]) => typeof value === "string").map(([key,value]) => `<div><dt>${escapeHtml(humanize(key))}</dt><dd>${escapeHtml(humanize(value))}</dd></div>`).join("")}</dl><p>Service-role and secret keys are server-only and absent from desktop fixtures. Supabase RLS status is metadata, not proof of authorization.</p></section>`; }

function renderAudit(state, escapeHtml, humanize) { return `<section class="settings-card"><h2>Safe audit history</h2><div class="audit-list">${state.audit.map((item) => `<article><time>${escapeHtml(formatDateTime(item.timestamp))}</time><div><strong>${escapeHtml(humanize(item.action))}</strong><small>${escapeHtml(item.actorClassification)} · ${escapeHtml(item.resource)} · ${escapeHtml(humanize(item.outcome))}</small></div></article>`).join("")}</div></section>`; }

function renderSecretDeleteDialog(state, escapeHtml, escapeAttribute) { const review = state.secrets.pendingDelete; if (!review) return ""; return `<dialog class="settings-dialog" open aria-labelledby="secret-delete-title" aria-describedby="secret-delete-detail"><form method="dialog"><p class="eyebrow">Fixture deletion review</p><h2 id="secret-delete-title">Delete ${escapeHtml(review.name)} metadata?</h2><p id="secret-delete-detail">The synthetic value is already unavailable. This removes only deterministic fixture metadata and records a redacted audit event.</p><div><button type="button" data-settings-action="cancel_secret_delete">Cancel</button><button type="button" class="danger" data-secret-delete-confirm="${escapeAttribute(review.name)}">Confirm fixture deletion</button></div></form></dialog>`; }
function summary(label, value, detail, escapeHtml, humanize) { return `<article><span class="status-glyph" aria-hidden="true">●</span><div><small>${escapeHtml(label)}</small><strong>${escapeHtml(humanize(value))}</strong><p>${escapeHtml(humanize(detail))}</p></div></article>`; }
function sectionLabel(id) { return ({ overview:"Overview", account:"Account", workspace:"Workspace", ai_models:"AI / Models", environment:"Project Environment", secrets:"Secrets", database:"Database / Supabase", stripe:"Stripe", github:"GitHub", integrations:"External Integrations", storage:"Storage", usage:"Usage links", security:"Security", desktop_local:"Desktop / Local" })[id] || id; }
function formatDateTime(value) { const date = new Date(value); return Number.isNaN(date.valueOf()) ? "Unknown" : date.toISOString().replace("T", " ").slice(0, 16) + " UTC"; }

function settingsStyles() { return `.settings-page{display:grid;gap:16px}.settings-boundary{display:flex;gap:12px;align-items:center;padding:11px 13px;border:1px solid var(--line);border-radius:9px;background:var(--surface-3)}.settings-boundary span{color:var(--muted)}.settings-layout{display:grid;grid-template-columns:220px minmax(0,1fr);gap:14px}.settings-sections{display:grid;align-content:start;gap:3px;position:sticky;top:10px}.settings-sections button{text-align:left;border-color:transparent;background:transparent}.settings-sections button[aria-current=page]{background:var(--surface-3);border-color:var(--line);font-weight:750}.settings-content,.settings-stack{display:grid;gap:12px;min-width:0}.settings-card{border:1px solid var(--line);border-radius:10px;background:var(--surface-3);padding:14px}.settings-card h2{margin-top:0}.settings-summary{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:8px}.settings-summary article{display:flex;gap:8px;padding:11px;border:1px solid var(--line);border-radius:9px;background:var(--surface-3)}.settings-summary small,.settings-summary strong,.settings-summary p{display:block}.settings-summary p{margin:2px 0;color:var(--muted)}.settings-meta{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:10px}.settings-meta dt{font-size:11px;color:var(--muted)}.settings-meta dd{margin:2px 0;overflow-wrap:anywhere}.settings-note{padding:9px;border-left:3px solid var(--focus);background:var(--surface-2)}.button-row,.environment-tabs{display:flex;flex-wrap:wrap;gap:7px}.environment-tabs span{border:1px solid var(--line);border-radius:999px;padding:4px 8px}.metadata-table{display:grid;overflow:auto}.metadata-table>div{display:grid;grid-template-columns:minmax(180px,1.4fr) repeat(3,minmax(100px,1fr));gap:8px;padding:8px;border-top:1px solid var(--line)}.table-head{font-size:11px;color:var(--muted);font-weight:700}.compact-form{display:grid;grid-template-columns:repeat(3,minmax(150px,1fr));gap:10px;align-items:end}.compact-form label{display:grid;gap:4px}.compact-form small,.compact-form .button-row{grid-column:1/-1}.checkbox-label{display:flex!important;align-items:center}.secret-list,.provider-list,.integration-list,.change-list,.audit-list{display:grid}.secret-list article,.provider-list article,.integration-list article,.change-list article,.audit-list article{display:flex;gap:9px;align-items:center;border-top:1px solid var(--line);padding:10px 0}.secret-list article>div,.provider-list article>div,.integration-list article>div,.change-list article>div,.audit-list article>div{display:grid;flex:1}.secret-list small,.provider-list small,.integration-list small,.change-list small,.audit-list small{color:var(--muted)}.settings-review,.database-review{margin-top:10px;padding:12px;border:1px solid var(--line);border-radius:8px;background:var(--surface-2)}.database-review[role=dialog]{outline:2px solid transparent}.status-glyph{font-weight:800}.settings-dialog{position:fixed;inset:50% auto auto 50%;transform:translate(-50%,-50%);z-index:10;max-width:min(520px,calc(100vw - 32px));color:var(--text);background:var(--surface-3);border:1px solid var(--line);border-radius:12px;box-shadow:0 18px 70px rgba(0,0,0,.28);padding:20px}.settings-dialog::backdrop{background:rgba(20,18,30,.35)}.settings-dialog form>div{display:flex;justify-content:flex-end;gap:8px}.preference-list{display:grid;gap:8px}.preference-list label{display:flex;justify-content:space-between;gap:10px;padding:8px;border-top:1px solid var(--line)}.preference-list label span{display:grid}@media(max-width:1000px){.settings-summary{grid-template-columns:1fr 1fr}.settings-meta{grid-template-columns:1fr 1fr}}@media(max-width:760px){.settings-layout{grid-template-columns:1fr}.settings-sections{position:static;display:flex;overflow:auto}.settings-sections button{white-space:nowrap}.compact-form{grid-template-columns:1fr}.settings-summary,.settings-meta{grid-template-columns:1fr}.metadata-table>div{grid-template-columns:minmax(140px,1fr) repeat(3,minmax(90px,1fr))}}@media(prefers-reduced-motion:reduce){.settings-page *{scroll-behavior:auto!important;transition:none!important}}`;
}

module.exports = { renderSettings, settingsStyles };
