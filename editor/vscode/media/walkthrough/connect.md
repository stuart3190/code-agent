# Connect to Thrallo

1. Open the Thrallo web workspace and go to **Settings → API tokens**.
2. Create a token (it is shown once) and copy it.
3. Run **Thrallo: Connect** and paste the token — it is stored in the editor's secure secret storage, never on disk in plain text.
