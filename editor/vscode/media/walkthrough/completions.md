# Inline completions

Thrallo completions combine a bounded local index of your open workspace (built inside the editor — only the top three relevant excerpts ever leave your machine) with the encrypted repository index on the server. They spend tokens from your Thrallo allowance, so they are **off** until you enable `thrallo.completions.enabled`.
